/*
 * EdgeCube Daemon API
 *
 * EdgeCube v2 守护进程 API(唯一契约)。  - 契约优先:所有 API 变更先改本文件,再由生成器产出双端代码   (Rust daemon / Flutter client),任何一端不得绕过契约私自改协议。 - 实现阶段标记:每个 path 的 `x-phase` 标注落地阶段(0/1/2/3),对应   docs/PLAN.md §10 分阶段路线。 - WS 部分(events / terminal)不在 OpenAPI 能力范围内,见同目录 `ws.md`。 - 鉴权:除 /auth/_* 与 /health 外全部需要 `Authorization: Bearer <token>`;   6 位对码经 `auth/pair` 换取长期 token。 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ServerTypeInfo : 服务端类型静态定义。「下载服务端」向导数据源,分类结构与 V1 对齐。 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ServerTypeInfo {
    /// 类型 id,如 vanilla / paper / spigot / craftbukkit / purpur / leaf / leaves / velocity / bungeecord / fabric / pocketmine / powernukkitx / allay
    #[serde(rename = "type")]
    pub r#type: String,
    #[serde(rename = "category")]
    pub category: Category,
    /// true 时版本页选择 MC 版本后需经 /catalog/loaders 选加载器版本(目前仅 fabric)
    #[serde(rename = "hasLoader", skip_serializing_if = "Option::is_none")]
    pub has_loader: Option<bool>,
    /// 该类型默认落盘文件名(如 server.jar / bungeecord.jar / PocketMine-MP.phar);下载信息可覆盖
    #[serde(rename = "fileName")]
    pub file_name: String,
}

impl ServerTypeInfo {
    /// 服务端类型静态定义。「下载服务端」向导数据源,分类结构与 V1 对齐。 
    pub fn new(r#type: String, category: Category, file_name: String) -> ServerTypeInfo {
        ServerTypeInfo {
            r#type,
            category,
            has_loader: None,
            file_name,
        }
    }
}
/// 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Category {
    #[serde(rename = "vanilla")]
    Vanilla,
    #[serde(rename = "plugin")]
    Plugin,
    #[serde(rename = "mod")]
    Mod,
    #[serde(rename = "proxy")]
    Proxy,
    #[serde(rename = "bedrock")]
    Bedrock,
}

impl Default for Category {
    fn default() -> Category {
        Self::Vanilla
    }
}

