/*
 * EdgeCube Daemon API
 *
 * EdgeCube v2 守护进程 API(唯一契约)。  - 契约优先:所有 API 变更先改本文件,再由生成器产出双端代码   (Rust daemon / Flutter client),任何一端不得绕过契约私自改协议。 - 实现阶段标记:每个 path 的 `x-phase` 标注落地阶段(0/1/2/3),对应   docs/PLAN.md §10 分阶段路线。 - WS 部分(events / terminal)不在 OpenAPI 能力范围内,见同目录 `ws.md`。 - 鉴权:除 /auth/_* 与 /health 外全部需要 `Authorization: Bearer <token>`;   6 位对码经 `auth/pair` 换取长期 token。 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TaskKind : 任务类型。实例操作(start/stop/restart/kill)与归档操作(compress/extract) 按 instanceId 分组 FIFO 串行,同一实例同 kind 未结束的任务重复提交将被拒绝 (409 task_conflict)。analyze 为插件/模组元数据解析(后端解析,前端轮询后拉取结果)。 download_single_file 为通用单文件下载(每个任务内部绑定一个 aria2 下载任务 gid, 模组/插件等单文件下载复用):同一实例允许多个排队,按 instanceId 分组 FIFO 串行 执行(一个完成再下一个),允许覆盖同名目标文件,前端负责按目标路径去重。 
/// 任务类型。实例操作(start/stop/restart/kill)与归档操作(compress/extract) 按 instanceId 分组 FIFO 串行,同一实例同 kind 未结束的任务重复提交将被拒绝 (409 task_conflict)。analyze 为插件/模组元数据解析(后端解析,前端轮询后拉取结果)。 download_single_file 为通用单文件下载(每个任务内部绑定一个 aria2 下载任务 gid, 模组/插件等单文件下载复用):同一实例允许多个排队,按 instanceId 分组 FIFO 串行 执行(一个完成再下一个),允许覆盖同名目标文件,前端负责按目标路径去重。 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum TaskKind {
    #[serde(rename = "start")]
    Start,
    #[serde(rename = "stop")]
    Stop,
    #[serde(rename = "restart")]
    Restart,
    #[serde(rename = "kill")]
    Kill,
    #[serde(rename = "download")]
    Download,
    #[serde(rename = "export")]
    Export,
    #[serde(rename = "backup")]
    Backup,
    #[serde(rename = "compress")]
    Compress,
    #[serde(rename = "extract")]
    Extract,
    #[serde(rename = "analyze")]
    Analyze,
    #[serde(rename = "download_single_file")]
    DownloadSingleFile,

}

impl std::fmt::Display for TaskKind {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Start => write!(f, "start"),
            Self::Stop => write!(f, "stop"),
            Self::Restart => write!(f, "restart"),
            Self::Kill => write!(f, "kill"),
            Self::Download => write!(f, "download"),
            Self::Export => write!(f, "export"),
            Self::Backup => write!(f, "backup"),
            Self::Compress => write!(f, "compress"),
            Self::Extract => write!(f, "extract"),
            Self::Analyze => write!(f, "analyze"),
            Self::DownloadSingleFile => write!(f, "download_single_file"),
        }
    }
}

impl Default for TaskKind {
    fn default() -> TaskKind {
        Self::Start
    }
}

