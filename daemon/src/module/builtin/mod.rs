//! 内置模块。
//!
//! * [`core`] —— 框架自省与基础运维（常驻，卸不掉）。
//! * [`ticker`] —— **示例模块模板**：一个文件里把配置、HTTP 路由、WS 方法、
//!   后台任务、事件推送、优雅停止全用上了。照它抄就能加新模块。
//! * [`instance`] —— 实例管理（真正的业务模块）：元数据存储层单独拆了
//!   `store` 子模块，多文件模块照它这个结构组织。

pub mod core;
pub mod instance;
pub mod ticker;
